PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=secret
DB_NAME=order_mgmt

# Optional — leave empty to run without Redis
REDIS_URL=

# Optional: used if you want USD conversion on the frontend via a public API
# FRONTEND_URL=http://localhost:5173
