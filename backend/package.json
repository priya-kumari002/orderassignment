{
  "name": "order-management-backend",
  "version": "1.0.0",
  "description": "Order Management API — Node + Express + MySQL",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:init": "node scripts/init-db.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mysql2": "^3.11.0"
  },
  "optionalDependencies": {
    "ioredis": "^5.4.1"
  }
}
